package com.example.shilpakalashowcase.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────
//  SHILPA-KALA — Premium Dark Gold Palette
//  Inspired by ancient Indian sculpture & bronze
// ─────────────────────────────────────────────

// Backgrounds
val DeepCharcoal    = Color(0xFF12110C)   // Main background — near-black with warm tone
val RichBlack       = Color(0xFF1A1810)   // Surface — slightly lighter than background
val ElevatedSurface = Color(0xFF242218)   // Cards, modals — elevated surfaces
val SubtleSurface   = Color(0xFF2E2B1E)   // Subtle highlights, dividers

// Gold Accents — the hero colors
val AncientGold     = Color(0xFFD4A843)   // Primary gold — buttons, active states
val PaleGold        = Color(0xFFF0C866)   // Lighter gold — hover, highlights
val DeepGold        = Color(0xFF8B6914)   // Darker gold — pressed states, borders
val GoldGlow        = Color(0xFFD4A84333) // Semi-transparent gold — glows, overlays

// Text Colors
val WarmIvory       = Color(0xFFEDE0C4)   // Primary text — warm white
val SoftCream       = Color(0xFFB8A88A)   // Secondary text — muted
val MutedBronze     = Color(0xFF7A6842)   // Hint/placeholder text
val DisabledText    = Color(0xFF4A4535)   // Disabled state text

// Status Colors
val SuccessGreen    = Color(0xFF4CAF82)   // Success states
val ErrorTerracotta = Color(0xFFD4604A)   // Error / destructive
val WarnAmber       = Color(0xFFF0A030)   // Warning

// Neutral overlays
val ScrimDark       = Color(0xCC000000)   // 80% black — for dialogs, overlays
val CardBorder      = Color(0xFF3A3420)   // Subtle card border

